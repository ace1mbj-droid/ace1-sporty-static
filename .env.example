# Supabase Configuration
# Copy this file to .env and replace with your actual values
# Get these from: https://app.supabase.com → Your Project → Settings → API

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# Optional: server / admin keys (do NOT commit real keys to version control)
# - SUPABASE_KEY is a project-level key usable by trusted server code (if you keep one)
# - SUPABASE_SERVICE_ROLE_KEY is a powerful service-role key intended for server/admin tasks
#   and should be stored only in secure environments (CI secrets, server envs). Avoid
#   placing service-role keys in frontend or public places.
SUPABASE_KEY=your_server_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Razorpay Configuration (for payment gateway)
# Get these from: https://dashboard.razorpay.com/app/keys

RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Site Configuration
SITE_URL=http://localhost:3000
SITE_NAME=Ace1 E-Commerce
SUPPORT_EMAIL=support@ace1.com
